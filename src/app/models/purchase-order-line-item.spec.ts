import { PurchaseOrderLineItem } from './purchase-order-line-item';

describe('PurchaseOrderLineItem', () => {
  it('should create an instance', () => {
    expect(new PurchaseOrderLineItem()).toBeTruthy();
  });
});
