import { CreateNewMemberReq } from './create-new-member-req';

describe('CreateNewMemberReq', () => {
  it('should create an instance', () => {
    expect(new CreateNewMemberReq()).toBeTruthy();
  });
});
