import { SelectedPartChoicePair } from './selected-part-choice-pair';

describe('SelectedPartChoicePair', () => {
  it('should create an instance', () => {
    expect(new SelectedPartChoicePair()).toBeTruthy();
  });
});
